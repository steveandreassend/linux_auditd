# Record the execve system call for users above UID 500 to catch all program executions
# This will log all OS commands issued by DBAs and their scripts
-a exit,always -F arch=b64 -S execve -F auid>=500
-a exit,always -F arch=b32 -S execve -F auid>=500
